import styled from 'styled-components'
import { FormControl, FORM_CONTROL_BORDER_RADIUS } from '../shared'

export const Input = styled(FormControl)`
  ::placeholder,
  ::-webkit-input-placeholder {
    color: #b1b5c3;
  }
`
